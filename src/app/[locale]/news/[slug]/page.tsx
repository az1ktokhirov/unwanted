export default function NewsDetailPage({ params }: { params: Promise<{ slug: string }> }) {
  return <main className="min-h-screen bg-[#1a1a2e] text-white p-8"><h1 className="font-bebas text-4xl">News Detail — coming in Stage 7</h1></main>;
}

export default function PlayerPage({ params }: { params: Promise<{ id: string }> }) {
  return <main className="min-h-screen bg-[#1a1a2e] text-white p-8"><h1 className="font-bebas text-4xl">Player Profile — coming in Stage 4</h1></main>;
}
